# 直连协议逆向实验记录

> 状态: 进行中 — 详情请求(0000005c)尚未拿到 cmd=92 数据
> 最后更新: 2026-08-08 15:45

## 目标

绕过游戏客户端翻页,直接 TCP 连接服务器(45.253.138.71:8001)拉取完整战报(cmd=92)入库。

## 帧结构(已确认,基于 requests/responses\_debug.log 对照)

### 请求帧

| 偏移 | 长度 | 内容                                             |
| -- | -- | ---------------------------------------------- |
| 0  | 4  | 包大小(不含这4字节)                                    |
| 4  | 4  | 常量 `00 00 AF 47`(44871)                        |
| 8  | 4  | 版本/session 字段(如 `000032DB`, `00003AC1`, 随会话变化) |
| 12 | 32 | 玩家ID(ASCII hex,32字节)                           |
| 44 | 4  | **mode/子命令**(如 `0000005C`)                     |
| 48 | 4  | **counter**(全局单调递增,per-player,跨会话连续)           |
| 52 | 3  | sub3(每命令固定,如详情=0945DA)                         |
| 55 | 1  | XOR 密钥(随机)                                     |
| 56 | 1  | 固定 `05`                                        |
| 57 | n  | 载荷,用密钥逐字节 XOR                                  |

### 响应帧

| 偏移 | 长度 | 内容                                                         |
| -- | -- | ---------------------------------------------------------- |
| 0  | 4  | 包大小                                                        |
| 4  | 4  | cmd(回显请求 mode:92→0000005C;2100→00000834;或 ack:90007/90008) |
| 8  | 12 | ?(会话相关)                                                    |
| 12 | 1  | 类型: `03`=zlib(载荷从17开始,789C魔数) / `05`=XOR 0x98 加密           |
| 13 | n  | 载荷(仅 type5)                                                |
| 17 | n  | zlib 数据(仅 type3)                                           |

## 命令映射表(已确认)

| mode       | 含义       | 载荷                                       | 响应                                           |
| ---------- | -------- | ---------------------------------------- | -------------------------------------------- |
| `000010E9` | 打开战报面板   | `[<主城ID>, 0]`                            | 90008                                        |
| `000010EA` | 打开战报列表   | `[<cityID>]`                             | 90007                                        |
| `000002B6` | 列表重置/切同盟 | `[]`                                     | **cmd=2100 列表数据**(XOR0x98,`[battle_id,...]`) |
| `0000005C` | 战报详情     | `[[1, 2], [1, 3], [-1], <battle_id>, 0]` | **cmd=92 完整战报**(zlib,含多条 `battle_id`/`time`) |
| `0000000A` | 批量详情     | 二进制                                      | cmd=10                                       |
| `00015F99` | 心跳/翻页    | 游标                                       | 90008(14B纯ack)                               |
| `00015F96` | 翻页       | `[<游标>]`                                 | 90007(12B ack)                               |

**重要纠错**:90007/90008 不是"拒绝"!游戏自己发 `000010EA/000002B6/00015F96` 也收到 90007,只是各命令的正常 ack。**但详情请求(0000005C)的 battle\_id 无效时返回 90007**。

**重要纠错2**:cmd=2100 是**盟聊/世界频道消息列表**(实测:消息ID+玩家名+聊天内容+时间戳),**不是战报列表**!战报ID只能从 90005 的 `Tb_battle_report` 记录或 cmd=92 响应中获取。

## 关键发现

- 确认完整请求布局(offset 44 才是 mode,offset 48 是 counter,此前的 \[4:8]=cmd 理解错误)
- **cmd=92 实测配对**: 发 `0000005C`(counter=00B69BB8, sub3=0945DA)→ 89ms 后 1收到 cmd=92(len 3940, zlib)
- **cmd=2100 实测配对**: 000002B6 → 2100,载荷 `[39845, 0, 0, 12094, "标题", "摘要", 时间戳, ...]`,首字段=battle\_id
- **90007/90008 是正常 ack**(游戏自己也收到,非拒绝)
- **counter 语义**: per-player 全局单调递增,游戏约 27 请求/秒。测试若用已用过的 counter → 请求被服务器静默丢弃(详情 0 响应根因)。counter = 最新捕获+1 即可(counter+10000 偏移也可)
- **sub3 随会话变化**: sub3=0945DA,sub3=06D436——必须用当前会话捕获帧的 sub3
- **详情请求必须用真实 battle\_id** 
  - 游戏发 battle\_id=75473370 → **19ms 后 cmd=92** ✓
  - 测试发 battle\_id=9999999999 → **90007** ✗
  - 000010e9 打开面板**不是**详情前置条件
  - 方案: 测试首请求用缓存 detail 帧里游戏真实查看的 battle\_id,后续锚点递减

## 当前代码状态(fetch.go)

已实现:

1. `buildOpenPanelRequest` / `buildListResetRequest` / `buildDetailRequest` / `buildPageRequest` — 帧构造与真实帧一致
2. `DirectFetchTest` 流程: 登录 → **解析98888绑定新会话身份** → 批量前置(0000000A) → 详情(0000005C)
3. `DirectFetchLoop` 后台循环: 同样序列 + 入库
4. **counter 起始 = 模板最新 counter + 10000**(避开与游戏并发撞车)
5. 详情首请求 battle\_id: **优先用 getDetailBattleID()(游戏真实查看的战报ID)**,收到 cmd=92 后锚点=最小battle\_id-1 递减
6. `parseReportListIDs` — 从 90005 的 Tb\_battle\_report 提取真实战报ID(战报ID唯一可靠来源)
7. open panel 参数(000010E9帧)持久化到 directfetch\_cache.json,重启不丢,且校验玩家ID匹配
8. **测试输出过滤聊天帧**(2100 盟聊/2200 同盟信息,确认是聊天消息非战报)

第三轮会话重绑定(2026-08-08 17:20,依据《第三轮联调分析与下一步实施指导.md》):

1. **98888 解析**: `[12:16]=version(与[52:56]重复一致)` `[17:20]=counter seed(3字节大端)` `[20:52]=playerID(32字节ASCII hex)`
2. **实测验证**:  98888 seed=0xef31bf → 13:42:00 首业务帧 counter=0xef31c3(**seed+4**);playerID 每次登录都变(连接A=2a2f9c…, 连接B=a7969c…, 15:57=0891d9e366be5da7/61ec81db…)
3. **LoginSession 重绑定**: 业务帧全部用 98888 下发的 version/playerID, counter 从 seed+4 起递增,不再复用旧模板身份
4. **登录同步等待**: 改为"空闲窗口≥800ms"(最长8s),不再固定3s
5. **阶段状态机**: batch(0000000A)→必须收到cmd=10才发详情;详情=零锚点(0)→真实锚点(游戏查看的战报ID)
6. 移除 init 帧重放(旧连接原始帧带旧身份,重放有害);随机游标翻页已删除

<br />

## 日志文件

- `build/bin/requests_debug.log` — 客户端→服务器请求(含完整hex)
- `build/bin/responses_debug.log` — 服务器→客户端响应
- `build/bin/fetch_debug.log` — 直连测试调试输出
- `build/bin/directfetch_cache.json` — 捕获模板缓存(login帧/init帧/详情帧/counter/sub3)

